# EduFacil

```mermaid
    erDiagram
    Rol {
        Nombre VARCHAR(45)
        IdRol INT PK
    }

    Usuario {
        NombreUsuario VARCHAR(45)
        Nombre VARCHAR(45)
        Apellido VARCHAR(45)
        Email VARCHAR(75)
        Password VARCHAR(45)
        idUsuario INT PK
    }
```